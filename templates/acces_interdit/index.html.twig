{% extends 'base.html.twig' %}

{% block title %}Acces Interdit{% endblock %}

{% block body %}

    <div class="container-fluid h-100">
        <div class="row h-100">
            <div class="col-lg-6 m-auto text-center">
                <img alt="Logo Limayrac" src="{{ asset('images/logo.png') }}" class="rounded mx-auto d-block img-fluid">
                <h1 class="h3 mb-3 font-weight-normal"><b>Accès interdit</b></h1>
                <a href="{{ path('home') }}">Cliquez ici pour retourner à l'accueil</a>

            </div>
        </div>
    </div>

{% endblock %}